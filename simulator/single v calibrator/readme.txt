calibrator.cpp   -> error function is x,y and theta so v_x*ti, v_y*ti and w*ti
                    it uses  calibrator.h the test  script called calibrator_test.cpp

calibrator_v.cpp -> error function is v,w so (s/t) amd (theta/t)
                 -> it uses the same .h and same test script called calibrator_test.cpp
